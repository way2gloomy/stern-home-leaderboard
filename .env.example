# =============================================================================
# Stern Home Leaderboard Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED - Stern Pinball Account Credentials
# -----------------------------------------------------------------------------
# Your Stern Pinball account username and password
# These are required for the backend to authenticate with Stern's API
# Prefer using a secret file on the host or in Docker rather than storing cleartext here.
STERN_USERNAME=your_stern_username
# IMPORTANT: If your password contains special characters (e.g. $, !, &), wrap it in double quotes
# Example: STERN_PASSWORD="pa$$word!with$special&chars"
STERN_PASSWORD="your_stern_password"

# Optional: read credentials from a file instead of storing them in this file.
# This is useful for Docker or host-managed secrets.
# Example (Docker): mount a file and set the path here.
# STERN_USERNAME_FILE=/run/secrets/stern_username
# STERN_PASSWORD_FILE=/run/secrets/stern_password

# Optional: set a strong session secret instead of letting the app generate one per start.
# SESSION_SECRET=replace-with-a-long-random-string

# Optional: restrict which origins can call the backend. Keep this tight for LAN use.
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://raspberrypi:3000

# -----------------------------------------------------------------------------
# BACKEND CONFIGURATION
# -----------------------------------------------------------------------------
# Backend server port (default: 5100)
PORT=5100

# Default location settings for API requests
# These are used when making requests to Stern's API
DEFAULT_COUNTRY=US
DEFAULT_STATE=CO
DEFAULT_STATE_NAME=Colorado
DEFAULT_CONTINENT=NA

# -----------------------------------------------------------------------------
# FRONTEND CONFIGURATION
# -----------------------------------------------------------------------------
# Frontend server port (default: 3000)
# Change this if you have conflicts with other services on port 3000
FRONTEND_PORT=3000

# Data refresh interval in minutes (default: 60)
# WARNING: Setting this too low (< 30 minutes) may cause account lockouts
VITE_DATA_REFRESH_INTERVAL_MINUTES=60

# Grid layout configuration
# Number of columns for machine cards (default: 1)
# Examples:
# VITE_GRID_COLUMNS=1  # Single column (default)
# VITE_GRID_COLUMNS=2  # Two columns side by side
# VITE_GRID_COLUMNS=3  # Three columns side by side
# VITE_GRID_COLUMNS=X  # X columns (auto-reduced on smaller screens)
VITE_GRID_COLUMNS=1

# -----------------------------------------------------------------------------
# Optional: Disable automatic scrolling in the UI (set to true to disable)
# Remove the leading '#' to enable this option
# VITE_DISABLE_AUTOSCROLL=true
# -----------------------------------------------------------------------------
# CUSTOM DATA & ASSETS CONFIGURATION
# -----------------------------------------------------------------------------
# Path to custom CSS file for styling overrides
# This should be the path inside the Docker container's data volume
# The data volume is mounted at /app/data and can contain:
# - CSS files for custom styling
# - Images (backgrounds, logos, etc.)
# - Fonts for custom typography
# - Other static assets
#
# Example paths:
# CUSTOM_CSS_PATH=/app/data/custom.css
# CUSTOM_CSS_PATH=/app/data/styles/overrides.css
CUSTOM_CSS_PATH=/app/data/custom.css

# Data volume mount path (inside container)
# This is where user assets will be mounted
DATA_VOLUME_PATH=/app/data
